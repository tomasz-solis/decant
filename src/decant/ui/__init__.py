"""UI components for the Streamlit app.

Modules in this package render Streamlit UI fragments — sidebar
widgets, auth forms, chart panels. They depend on
`decant.supabase_session` and `decant.services` but contain no business
logic of their own; they're presentation only.
"""
